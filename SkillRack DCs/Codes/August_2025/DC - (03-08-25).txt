Problem Statement :-

Insert Substring - Balanced Parentheses in DC (Daily Challenge) in SkillRack in Python - Programming

Code :-

parentheses_string_1 = input().strip()

N = len(parentheses_string_1)

parentheses_string_2 = input().strip()

M = len(parentheses_string_2)

current_depth = 0

maximum_depth = 0

maximum_depth_index = 0

for index in range(0 , N):
    
    if(parentheses_string_1[index] == '('):
        
        current_depth += 1
    
    elif(parentheses_string_1[index] == ')'):
        
        if(maximum_depth < current_depth):
            
            maximum_depth = current_depth
            
            maximum_depth_index = index
        
        current_depth -= 1

left_string = parentheses_string_1[0 : maximum_depth_index]

middle_string = parentheses_string_2

right_string = parentheses_string_1[maximum_depth_index : N]

new_parentheses_string = (left_string + middle_string + right_string)

print(new_parentheses_string)
