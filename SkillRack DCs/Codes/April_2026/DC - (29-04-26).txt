Problem Statement :-

First And Last Odd Digits in DC (Daily Challenge) in SkillRack in Python - Programming

Code :-

import math

def get_length_of_N(N):
    
    return ((0) if(N == 0) else ((int(math.log10(abs(N)))) + 1))

def is_odd(N):
    
    return ((N & 1) == 1)

def compute_power_function_in_log_N_time_complexity(base_value , power_value):
    
    product_value = 1
    
    while(power_value > 0):
        
        product_value = (product_value * base_value) if(is_odd(power_value)) else (product_value)
        
        base_value = (base_value * base_value)
        
        power_value = (power_value // 2)
    
    return product_value

def get_first_digit(number , length_of_number):
    
    return (number // (compute_power_function_in_log_N_time_complexity(10 , (length_of_number - 1))))

def get_last_digit(number):
    
    return (number % 10)

(lambda N : [print(number , end = " ") for number in list(map(int , input().split())) if((is_odd(get_first_digit(number , get_length_of_N(number)))) and (is_odd(get_last_digit(number))))]) (int(input()))
