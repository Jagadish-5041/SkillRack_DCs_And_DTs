Problem Statement :-

Rotation Count In Anti-Clockwise Direction in DC (Daily Challenge) in SkillRack in Python - Programming

Code :-

N = int(input().strip())

values_list = list(map(int , input().strip().split()))

for index in range(0 , (N - 1)):
    
    if(values_list[index] > values_list[(index + 1)]):
        
        print((index + 1))
        
        quit()

print(0)
