Problem Statement :-

Function - areConsecutive() in DC (Daily Challenge) in SkillRack in C - Programming

Code :-

#include <string.h>

int is_consecutive(long long int A , long long int B){
    
    return (((A + 1) == B) || ((A - 1) == B));
}

int areConsecutive(char *digits_string){
    
    for(int N = strlen(digits_string) , index = 1 ; (index < N) ; index++){
        
        if(digits_string[index] != '0'){
            
            char *left_half_number_string = ((char *) malloc((N * sizeof(char))));
            
            sprintf(left_half_number_string , "%.*s" , index , &digits_string[0]);
            
            char *right_half_number_string = ((char *) malloc((N * sizeof(char))));
            
            sprintf(right_half_number_string , "%s" , &digits_string[index]);
            
            if(is_consecutive(atoll(left_half_number_string) , atoll(right_half_number_string))){
                return 1;
            }
        }
    }
    
    return 0;
}
