Problem Statement :-

Flowchart Logic 054 in DC (Daily Challenge) in SkillRack in Python - Programming

Code :-

M , N = map(int , input().split())

while(M < N):
    
    print(M , end = " ")
    
    if(((N % 10) != 0) and ((N % 2) != 0)):
        M += (N % 10)
    else:
        M += 1
    
    N -= 1
