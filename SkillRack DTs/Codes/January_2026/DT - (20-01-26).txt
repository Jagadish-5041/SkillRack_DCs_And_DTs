Problem Statement :-

Toy Watch - Color Band in DT (Daily Test) in SkillRack in Python - Programming

Code :-

N = int(input())

bands_list = list(map(int , input().split()))

hours_string , minutes_string = input().strip().split(":")

starting_minutes = 0

current_minutes = int(minutes_string)

flag = False

answer_position = -999

answer_starting_minutes = -999

answer_ending_minutes = -999

for index in range(0 , N):
    
    ending_minutes = ((starting_minutes + bands_list[index]) % 60)
    
    if((current_minutes == starting_minutes) or (current_minutes == ending_minutes)):
        print(-1)
        quit()
    
    if(starting_minutes < ending_minutes):
        if(starting_minutes < current_minutes < ending_minutes):
            flag = True
            answer_position = (index + 1)
            answer_starting_minutes = starting_minutes
            answer_ending_minutes = ending_minutes
            break
    elif((starting_minutes < current_minutes) or (current_minutes < ending_minutes)):
        flag = True
        answer_position = (index + 1)
        answer_starting_minutes = starting_minutes
        answer_ending_minutes = ending_minutes
        break
    
    starting_minutes = ending_minutes

answer_starting_hours = int(hours_string)

answer_ending_hours = (1) if((answer_ending_minutes == 0) and (answer_starting_hours == 12)) else ((12) if((answer_ending_minutes == 0) and (answer_starting_hours == 11)) else (((answer_starting_hours + 1) % 12) if(ending_minutes == 0) else (answer_starting_hours)))

print(answer_position , "{:02d}:{:02d} {:02d}:{:02d}".format(answer_starting_hours , answer_starting_minutes , answer_ending_hours , answer_ending_minutes))
