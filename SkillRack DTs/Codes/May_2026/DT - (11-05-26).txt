Problem Statement :-

Consonants In The Odd Position in DT (Daily Test) in SkillRack in Python - Programming

Code :-

def is_odd(N):
    
    return ((N & 1) == 1)

def is_consonant(character):
    
    return ((character.isalpha()) and (character not in "aeiouAEIOU"))

def get_ASCII_value(alphabet):
    
    return ord(alphabet)

[print(character.lower() , get_ASCII_value(character.lower()) , character.upper() , get_ASCII_value(character.upper())) for index , character in enumerate(input().strip()) if((is_odd((index + 1))) and (is_consonant(character)))]
