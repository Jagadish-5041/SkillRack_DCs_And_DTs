Problem Statement :-

Jumbled Program - 012 in DT (Daily Test) in SkillRack in C - Programming

Code :-

#include <stdio.h>
#include <stdlib.h>
#define jagadish_DT main

int is_odd(int N){
    
    return ((N & 1) == 1);
}

int jagadish_DT(){
    
    int N;
    
    scanf("%d", &N);
    
    int flag = 1;
    
    while(N > 0){
        
        if(is_odd(N)){
            
            flag = 0;
            
            printf("%d " , N);
        }
        
        N = (N / 10);
    }
    
    (flag == 1) ? (printf("-1")) : (printf(""));
    
    return 0;
}
